# Example only. Create the real Secret through your secret manager; do not commit credentials.
apiVersion: v1
kind: Secret
metadata:
  name: pipeline-secrets
  namespace: self-healing-pipeline
type: Opaque
stringData:
  OPENAI_API_KEY: ""
  GEMINI_API_KEY: ""
  # Add SASL credentials here only when the referenced Kafka cluster requires them.
  KAFKA_SASL_USERNAME: ""
  KAFKA_SASL_PASSWORD: ""
